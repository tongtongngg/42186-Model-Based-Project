[project]
name = "42186-model-based-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyro-ppl>=1.9.1",
    "torch>=2.11.0",
]
